# Packages

Shared packages used by multiple apps and services.

## Planned packages

- `ui` - shared UI components
- `types` - shared TypeScript types and API contracts
- `prompts` - prompt templates and AI behaviour definitions
- `sdk` - future public or internal client SDK

Packages should contain reusable logic only. App-specific logic belongs in apps.
