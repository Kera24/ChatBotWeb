# Infrastructure

Infrastructure definitions for local development and future deployment.

## Planned folders

- `docker` - Dockerfiles and compose files
- `kubernetes` - future Kubernetes manifests or Helm charts
- `terraform` - future cloud infrastructure definitions

MVP priority is local Docker-based development before cloud automation.
